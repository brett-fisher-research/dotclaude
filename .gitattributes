# Normalize line endings: store as LF in the repo and check out as LF
# everywhere, so the working tree stays consistent across machines.
* text=auto eol=lf

# Windows scripts that genuinely require CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
